services:
  db:
    image: postgres:18.3
    restart: always
    environment:
      POSTGRES_DB: vanilla_db
      POSTGRES_USER: vanilla_admin
      POSTGRES_PASSWORD: vanilla_admin
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: